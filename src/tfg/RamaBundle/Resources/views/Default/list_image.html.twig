{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    <div>
        <strong>{{ object }}</strong> <br />
        {% for rama in object.getDisciplinas() %}
          {{ rama.nombre }}
        {% endfor %}
    </div>
{% endblock %}
